<h1 id="all">All products</h1>

<!-- <ul>
  <% @products.each do |product| %>
    <li><%= link_to "#{product.name}", "/products/#{product.id}" %> - <%= product.description %> // <%= product.formatted_price %></li>
  <% end %>

<br>
	<div class="newp">
  <%= link_to "Create a new product", new_product_path %>
  	</div>
</ul> -->

<%= form_tag products_path, method: :get, authentication: false, id: 'search-form' do %>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Search" %>
<% end %>

<div id="products">
  <%= render @products %>
</div>